"""Agent 包：LangGraph 编排的岗位问答智能体。

rag 包解决"知识从哪来"（检索），本包解决"决策权在谁手里"：
模型在 ReAct 循环（思考 → 调工具 → 看结果 → 继续）中
自主决定用哪个工具、查几轮；护栏（步数上限 + 降级总结）防止失控。

设计取舍：不用 langchain 的模型封装，直接手写 messages 流转 + 硅基流动
OpenAI 兼容接口的 tools 参数——链路每一环自己可控，面试时能讲到字节级。
"""

__version__ = "0.1.0"
